# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant::Config.run do |config|
  config.vm.box     = "ubuntu-server"
  config.vm.box_url = "https://dl.dropbox.com/u/18054305/ubuntu-server.box"
  config.vm.share_folder "v-root", "/home/vagrant/squeeze", "."
end
